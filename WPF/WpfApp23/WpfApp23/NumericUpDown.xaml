<UserControl x:Class="WpfApp23.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp23"
             mc:Ignorable="d" 
             x:Name="root"
             d:DesignHeight="50" d:DesignWidth="125">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition ></ColumnDefinition>
            <ColumnDefinition Width="0.5*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBox Grid.RowSpan="2" x:Name="textbox" TextChanged="textbox_TextChanged"></TextBox>
        <Button Grid.Column="1" x:Name="lower_Btn" Click="lower_Btn_Click">UP</Button>
        <Button Grid.Column="1" Grid.Row="1" x:Name="upper_btn" Click="upper_btn_Click">DOWN</Button>
        
        
        
        
        
    </Grid>
</UserControl>
